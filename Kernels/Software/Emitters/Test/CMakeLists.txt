IF(WRAP_VTK)
	FIND_PACKAGE(VTK REQUIRED)
	INCLUDE_DIRECTORIES(${VTK_INCLUDE_DIRS})

	ADD_EXECUTABLE(Test_Isotropic Test_Isotropic.cpp VTKPointCloud.cpp)
	TARGET_LINK_LIBRARIES(Test_Isotropic ${Boost_UNIT_TEST_FRAMEWORK_LIBRARY} vtkIOLegacy vtkCommonDataModel)
ENDIF()
